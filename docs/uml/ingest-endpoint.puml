@startuml ingest-endpoint

database src as "Source" #line.dashed;

interface endpoint [
Endpoint
]

node backend [

Backend

]

database database [

Database

]

endpoint <<-->> backend : " HTTP Server (<b>/api/v1/ingest</b>)"

backend -->> src : " OAI-PMH / S3"
backend -->> backend : " JSON Schema \n Start Async Ingest"
backend -->> database : " HTTP Client (<b>/command</b>)"
database -->> database : " Interconnect"

@enduml
