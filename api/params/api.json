{
  "$schema": "https://json-schema.org/draft/2020-12/schema", "$id": "dataasee:api?params=api",

  "title": "DatAasee API Endpoint Parameters", "description": "The api endpoint returns the DatAasee OpenAPI specification for no parameter, or the respective parameter, request body and response schemas given the endpoint as parameter value.",

  "type": "object", "properties": {

    "params": { "type": "string", "title": "URI Parameter", "enum": ["api", "database", "schema", "metadata"] },

    "request": { "type": "string", "title": "Request Body", "enum": ["ingest"] },

    "response": { "type": "string", "title": "Response Body", "enum": ["api", "database", "health", "ingest", "metadata", "ready", "schema"] } }
}
