# Language: Benthos YAML, Project: DatAasee, License: MIT, Author: Christian Himpe

processor_resources:
  - label: "format_datacite"
    try:
      - label: 'format_datacite__get_payload'
        mapping: 'root = if this.exists("oai_datacite") { this.oai_datacite.payload.resource } else { this.resource }'

      - label: 'format_datacite__patch_sourceid'
        mapping: 'meta sourceid = if metadata("sourceid") == null { this.identifier."#text".catch(deleted()) }'

      - label: 'format_datacite__parser_workflow'
        workflow:
          meta_path: ''
          order:
            - - 'rawMetadata'
              - 'rawFormat'
              - 'dataLocation'
              - 'dataFormat'
              - 'publisher'
              - 'publicationYear'
              - 'resourceType'
              - 'language'
              - 'version'
              - 'license'
              - 'title'
              - 'keywords'
              - 'description'
              - 'rights'
              - 'creators'
              - 'identifiers'
              - 'synonyms'
              - 'subjects'
              - 'fundings'
              - 'relatedItems'
              - 'related'
            - - 'identifiers_1'
              - 'related_1'
            - - 'gather'
          branches:

            # Raw String

            'rawMetadata':
              request_map: 'root = this.without("dataasee")'
              processors:
                - mapping: |
                    root.rawMetadata = { "resource": this }.format_xml(no_indent: true).string()
              result_map: 'root.dataasee.rawMetadata = this.rawMetadata'

            # Raw Format

            'rawFormat':
              request_map: 'root = {}'
              processors:
                - mapping: |
                    root.rawFormat = "datacite+xml"
              result_map: 'root.dataasee.rawFormat = this.rawFormat'

            # First String

            'dataLocation':
              request_map: 'root = this.identifier.not_null().array().not_empty().catch(deleted())'
              processors:
                - mapping: |
                    let first = this.0
                    root.dataLocation = $first."#text" | $first.string()
              result_map: 'root.dataasee.dataLocation = this.dataLocation'

            'dataFormat':
              request_map: 'root = this.formats.format.not_null().array().not_empty().catch(deleted())'
              processors:
                - mapping: |
                    let first = this.0
                    root.dataFormat = $first."#text" | $first.string()
              result_map: 'root.dataasee.dataFormat = this.dataFormat'

            'publisher':
              request_map: 'root = this.publisher.not_null().array().not_empty().catch(deleted())'
              processors:
                - mapping: |
                    let first = this.0
                    root.publisher = $first."#text" | $first.string()
              result_map: 'root.dataasee.publisher = this.publisher'

            'publicationYear':
              request_map: 'root = this.publicationYear.not_null().array().not_empty().catch(deleted())'
              processors:
                - mapping: |
                    let first = this.0
                    root.publicationYear = $first."#text" | $first.string()
              result_map: 'root.dataasee.publicationYear = this.publicationYear'

            'resourceType':
              request_map: 'root = this.resourceType.not_null().array().not_empty().catch(deleted())'
              processors:
                - mapping: |
                    let first = this.0
                    root.resourceType = $first."-resourceTypeGeneral" | $first."#text" | $first.string()
              result_map: 'root.dataasee.resourceType = this.resourceType'

            'language':
              request_map: 'root = this.language.not_null().array().not_empty().catch(deleted())'
              processors:
                - mapping: |
                    let first = this.0
                    root.language = $first."#text" | $first.string()
              result_map: 'root.dataasee.language = this.language.not_empty().catch(deleted())'

            'version':
              request_map: 'root = this.version.not_null().array().not_empty().catch(deleted())'
              processors:
                - mapping: |
                    let first = this.0
                    root.version = $first."#text" | $first.string()
              result_map: 'root.dataasee.version = this.version.not_empty().catch(deleted())'

            'license':
              request_map: 'root = this.rightsList.rights.not_null().array().not_empty().catch(deleted())'
              processors:
                - mapping: |
                    let first = this.0
                    root.license = $first."-rightsURI" | $first."-rightsIdentifier" | $first."#text" | $first.string()
              result_map: 'root.dataasee.license = this.license.not_null().catch(deleted())'

            # Filtered String

            'title':
              request_map: 'root = this.titles.title.not_null().array().not_empty().catch(deleted())'
              processors:
                - mapping: |
                    let first = this.filter(x -> x.type() == "string" || !x.exists("-titleType")).0
                    root.title = $first."#text" | $first.string()
              result_map: 'root.dataasee.title = this.title'

            # Concatenate Strings

            'description':
              request_map: 'root = this.descriptions.description.not_null().array().not_empty().catch(deleted())'
              processors:
                - mapping: |
                    root.description = this.fold("",x -> match { x.value."-descriptionType" == "Abstract"               => x.value."#text",
                                                                 x.tally == "" && x.value."-descriptionType" == "Other" => x.value."#text",
                                                                 x.tally == "" && x.value.type() == "string"            => x.value,
                                                                 _ => x.tally })
              result_map: 'root.dataasee.description = this.description.not_empty().catch(deleted())'

            'rights':
              request_map: 'root = this.rightsList.rights.not_null().array().not_empty().catch(deleted())'
              processors:
                - mapping: |
                    let sep = " ¶ "
                    root.rights = this.map_each(x -> x."#text" | x.string()).join($sep)
              result_map: 'root.dataasee.rights = this.rights.not_empty().catch(deleted())'

            # Array

            'keywords':
              request_map: 'root = this.subjects.subject.not_null().array().not_empty().catch(deleted())'
              processors:
                - mapping: |
                    root.keywords = this.filter(x -> x."-valueURI" == null).map_each(x -> x."#text" | x.string())
              result_map: 'root.dataasee.keywords = this.keywords.not_empty().catch(deleted())'

            'creators':
              request_map: 'root = this.creators.creator.not_null().array().not_empty().catch(deleted())'
              processors:
                - mapping: |
                    root.creators = this.map_each(x -> { "name": x.creatorName."#text" | (if x.creatorName.type() == "string" { x.creatorName } else { x.familyName.string() + ", " + x.givenName.string() }),
                                                         "data": if x.exists("nameIdentifier") { match x.nameIdentifier.array().filter(y -> y.exists("-nameIdentifierScheme") && y."-nameIdentifierScheme".lowercase().re_match("^(?i)(orcid|ror)$")).not_empty().catch(x.nameIdentifier.array()).0 {
                                                                                                  this."#text".has_prefix("http") => this."#text".string(),
                                                                                                  this.exists("-schemeURI")       => this."-schemeURI".trim_suffix("/") + "/" + this."#text".string(),
                                                                                                  _                               => this."-nameIdentifierScheme" + ":"  + this."#text".string() }
                                                                                               } else { deleted() } })
              result_map: 'root.dataasee.creators = this.creators'

            'identifiers':
              request_map: 'root = this.identifier.not_null().array().not_empty().catch(deleted())'
              processors:
                - mapping: |
                    root.identifiers = [{"name": this.0."#text" | this.0.string(),
                                         "data": this.0."-identifierType" | "doi"}]
              result_map: 'root.dataasee.identifiers = this.identifiers'

            'synonyms':
              request_map: 'root = this.titles.title.not_null().array().not_empty().catch(deleted())'
              processors:
                - mapping: |
                    root.synonyms = this.filter(x -> x.exists("-titleType")).map_each(x -> {"name": x."#text",
                                                                                            "data": x."-titleType"})
              result_map: 'root.dataasee.synonyms = this.synonyms.not_empty().catch(deleted())'

            'subjects':
              request_map: 'root = this.subjects.subject.not_null().array().not_empty().catch(deleted())'
              processors:
                - mapping: |
                    root.subjects = this.filter(x -> x.exists("-valueURI")).map_each(x -> {"name": x."#text" | x.string(),
                                                                                           "data": x."-valueURI"})
              result_map: 'root.dataasee.subjects = this.subjects.not_empty().catch(deleted())'

            'fundings':
              request_map: 'root = this.fundingReferences.fundingReference.not_null().array().not_empty().catch(deleted())'
              processors:
                - mapping: |
                    root.fundings = this.map_each(x -> { "name": x.funderName."#text" | x.funderName.string(),
                                                         "data": x.awardNumber."-awardURI" | x.awardNumber."#text" | x.awardNumber | deleted() })
              result_map: 'root.dataasee.fundings = this.fundings'

            'relatedItems':
              request_map: 'root = this.relatedIdentifiers.relatedIdentifier.not_null().array().not_empty().catch(deleted())'
              processors:
                - mapping: |
                    root.relatedItems = this.filter(x -> x."-relatedIdentifierType".or("").re_match("^(?i)(url|doi|arxiv)$")).map_each(x -> {"name": x."-relationType",
                                                                                                                                             "data": match x."-relatedIdentifierType".lowercase() { "arxiv" => "https://arxiv.org/abs/" + x."#text",
                                                                                                                                                                                                    "doi"   => "https://doi.org/" + x."#text",
                                                                                                                                                                                                    "url"   => x."#text" }})
              result_map: 'root.dataasee.relatedItems = this.relatedItems.not_empty().catch(deleted())'

            # Object

            'related':
              request_map: 'root = this.relatedIdentifiers.relatedIdentifier.not_null().array().not_empty().catch(deleted())'
              processors:
                - mapping: |
                    root.related = this.fold({}, x -> x.tally.merge({ match x.value."-relationType" { "isNewVersionOf" => "isNewVersionOf",
                                                                                                      "isDerivedFrom"  => "isDerivedFrom",
                                                                                                      "isPartOf"       => "isPartOf",
                                                                                                      "hasPart"        => "hasPart",
                                                                                                      _                => "isRelatedTo" }: [x.value."#text"] }))
              result_map: 'root.dataasee.related = this.related.not_empty().catch(deleted())'

            # Additionals

            'identifiers_1':
              request_map: 'root = this.alternateIdentifiers.alternateIdentifier.not_null().array().not_empty().catch(deleted())'
              processors:
                - mapping: |
                    root.identifiers = this.map_each(x -> { "name": x."#text",
                                                            "data": x."-alternateIdentifierType" })
              result_map: 'root.dataasee.identifiers = this.identifiers.concat(root.dataasee.identifiers.or([]))'

            'related_1':
              request_map: 'root = this.relatedItems.relatedItem.not_null().array().not_empty().catch(deleted())'
              processors:
                - mapping: |
                    root.related = this.fold({}, x -> x.tally.merge({ match x.value."-relationType" { "isNewVersionOf" => "isNewVersionOf",
                                                                                                      "isDerivedFrom"  => "isDerivedFrom",
                                                                                                      "isPartOf"       => "isPartOf",
                                                                                                      "hasPart"        => "hasPart",
                                                                                                      _                => "isRelatedTo" }: x.value.relatedItemIdentifier.not_null().array().not_empty().map(y -> y."#text" | y.string()).catch(deleted()) }))
              result_map: 'root.dataasee.related = this.related.merge(root.dataasee.related.or({}))'

            # Finalize

            'gather':
              request_map: 'root = this'
              processors:
                - noop: {}
              result_map: 'root = this.dataasee'
